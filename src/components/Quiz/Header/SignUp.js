import { Box, Button } from '@mui/material';
import React from 'react';

const SignUp = () =>
	<Box className="signUp">
		<Button
			variant="contained"
			color="error"
		>
			Sign up
		</Button>
	</Box>;

export default SignUp;
