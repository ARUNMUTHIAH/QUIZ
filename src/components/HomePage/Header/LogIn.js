import { Box, Button } from '@mui/material';
import React from 'react';

const LogIn = () =>
	<Box className="logIn">
		<Button
			variant="contained"
			color="success"
		>
			Log in
		</Button>
	</Box>;

export default LogIn;
